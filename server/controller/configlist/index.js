const label = require('./label');
const addLabel = require('./addLabel');
const delLabel = require('./delLabel');
const patchLabel = require('./patchLabel');
const displayLabel = require('./displayLabel');

module.exports = {
    label,
    addLabel,
    delLabel,
    patchLabel,
    displayLabel,
};
