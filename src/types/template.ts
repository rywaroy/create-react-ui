export interface IDefaultFormValues {
  url: string;
  folderName: string;
  fileName: string;
  variable: string;
}

export interface IUmiFormValues {
  url: string;
  folderName: string;
  fileName: string;
  variable: string;
  namespace: string;
  oilConfig: boolean;
}

export interface ICustomFormValues {
  mode: string;
  url: string;
  folderName: string;
  fileName: string;
  variable: string;
}
